# Dashboard microservice (Tyler) — copy to `.env` and merge with repo root `.env` if needed.
# Convention: `REPLACE_ME` = populated in real `.env` (redact). `NULL` = placeholder in real `.env`.

SERVICE_NAME=dashboard-service
SERVICE_VERSION=local-dev
PORT=3005
HOST=0.0.0.0
NODE_ENV=REPLACE_ME
CORS_ORIGIN=REPLACE_ME

PGHOST=REPLACE_ME
PGPORT=REPLACE_ME
PGUSER=REPLACE_ME
PGPASSWORD=REPLACE_ME

PHONICS_DB=REPLACE_ME
COMPREHENSION_DB=REPLACE_ME
VISUALIZATION_DB=REPLACE_ME
AUDIOBOOK_DB=REPLACE_ME
DASHBOARD_DB=REPLACE_ME
