/** Client-only keys for the home → phonics workflow (sessionStorage). */
export const PHONICS_SESSION_STORY_TEXT_KEY = 'readon.phonics.storyText';
/** Shown once on `/` after redirect from `/phonics` when no story text was available. */
export const PHONICS_SESSION_HOME_FLASH_ERROR_KEY = 'readon.phonics.homeFlashError';
