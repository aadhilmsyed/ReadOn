steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - '$_DOCKERFILE_PATH'
      - '-t'
      - '$_IMAGE_URI'
      - '.'
images:
  - '$_IMAGE_URI'

options:
  logging: CLOUD_LOGGING_ONLY

